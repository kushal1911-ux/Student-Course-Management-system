server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Always fall back to index.html so React Router can handle client-side routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}